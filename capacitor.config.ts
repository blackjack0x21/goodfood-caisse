import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'goodfood-caisse',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
